import chai from 'chai';
import ChatWebAPIUtils from './ChatWebAPIUtils';

chai.should();

describe('tt', () => {
    describe('getAllMessages', () => {

        it('tt1', () => {
            let one=1;

            //assert
            one.should.equal(1);
        });
    });
});
